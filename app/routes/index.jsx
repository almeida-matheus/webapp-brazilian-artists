import React from 'react';
import {
	Router,
	Route,
	IndexRoute
} from 'react-router';

export function getRoutes () {
	return (
		<Router>
			<Route path="/" component={}>
			</Route>
		</Router>
	)
}